interface:
  display_name: "Invest Moat"
  short_description: "Map moat mechanisms to revenue-owned driver IDs"
  default_prompt: "Use $invest-moat to finalize captured-source moat mechanisms mapped to existing drivers into a validated artifact."
