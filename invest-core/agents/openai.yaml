interface:
  display_name: "Invest Core"
  short_description: "Validate shared contracts, driver refs, and lineage"
  default_prompt: "Use $invest-core to validate immutable investment artifacts and revenue-owned growth-driver references."
